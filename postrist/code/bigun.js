function buildDebugies() {
	var root = document.createElement('div');
	
	var camera = document.createElement('div');
		camera.id = "camera";
	
	root.appendChild(camera);
	
	return root;
}

function redrawComposition() {
	
}